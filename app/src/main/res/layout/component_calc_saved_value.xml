<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_weight="1"
    android:layout_marginEnd="18dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/valueText"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:background="@drawable/border_square"
        android:gravity="center"
        android:textSize="40sp"
        android:textStyle="bold"
        android:textColor="?colorOnBackground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageButton
        android:id="@+id/deleteButton"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="-10dp"
        android:layout_marginBottom="-10dp"
        android:padding="3dp"
        android:background="@drawable/calc_x_button"
        android:src="@drawable/ic_x"
        android:scaleType="fitCenter"
        app:tint="?clearStoredColor"
        android:contentDescription="@string/clear_cd"
        app:layout_constraintBottom_toTopOf="@+id/valueText"
        app:layout_constraintStart_toEndOf="@+id/valueText" />

</androidx.constraintlayout.widget.ConstraintLayout>
