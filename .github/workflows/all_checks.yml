name: All Checks

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  lint-test:
    name: "Lint and Test"
    uses: ./.github/workflows/basic_checks.yml
    secrets: inherit

  # TODO uncomment when repo is public
  # instrumented-tests:
    # name: "Instrumented Tests"
    # uses: ./.github/workflows/instrumented_tests.yml
    # secrets: inherit
